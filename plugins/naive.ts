import { createDiscreteApi } from "naive-ui";

export default defineNuxtPlugin(() => {
  // const {loadingBar, dialog} = createDiscreteApi(["loadingBar", "dialog"]);

  // return{
  //   //以下でreturnしているのは、provideというキーを持つオブジェクトです
  //   provide: {
  //     loadingBar,
  //     dialog,
  //   }
  // }
})
